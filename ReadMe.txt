Firebase->Authentication->Settings
   User account linking: Create multiple accounts
   for each identity provider

   Authorized domains: Add all applicable domains

For Auto-refresh add .ENV file->FAST_REFRESH=false

Twitter has issue with the Authentication so far.
No issues with Google and Github (see account linking)

NOW is Vercel now. The rest is the same.